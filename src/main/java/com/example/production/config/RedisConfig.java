package com.example.production.config;

import org.springframework.context.annotation.Configuration;

// Spring Boot tự cấu hình Redis từ application.yaml:
//   spring.data.redis.host / port
// Không cần khai báo thêm gì ở đây.
@Configuration
public class RedisConfig {
}
